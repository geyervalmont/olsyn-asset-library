apiVersion: v1
kind: Service
metadata:
  name: prismfs-studio-share-metrics
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: prismfs
    app.kubernetes.io/instance: studio-share
  ports:
    - name: metrics
      port: 9090
      targetPort: metrics
