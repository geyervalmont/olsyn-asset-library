# Placeholder. Replace with a sealed/external secret; never commit real values.
apiVersion: v1
kind: Secret
metadata:
  name: prismfs-studio-share
type: Opaque
stringData:
  # Drive token from the control plane (Drives page). Authenticates the
  # manifest fetch and the access-event posts.
  PRISMFS_MANIFEST_TOKEN: replace-me
  # Read-only object store credentials scoped to the bucket.
  AWS_ACCESS_KEY_ID: replace-me
  AWS_SECRET_ACCESS_KEY: replace-me
